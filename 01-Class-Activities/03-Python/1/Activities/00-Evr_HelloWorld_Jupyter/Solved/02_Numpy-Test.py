# Test pip install (run this in the terminal)
# pip install numpy

# Test importing a library
import numpy as np
tbl = np.arange(20).reshape(4,5)

# Preview a numpy object (just a test -- we will explain this later on)
print(tbl)