-- Create the subquery

-- Retrieve the same resuls using a join (hint: use INNER JOIN and GROUP BY)

-- Create View (create the view using either the subquery or join approach)

-- Query the view to the titles with 7 copies
